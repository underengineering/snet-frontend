import { FC, useState } from "react"
import { Api } from "@/lib/api";

interface Props {
    friends: Api.Users.IPrivateUser
}

const FriendList: FC<Props> = ({ }) => {
    return <div>
        wtf
    </div>;
}

export default FriendList;
